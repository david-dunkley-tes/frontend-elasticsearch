export function DemoBanner() {
  return (
    <div className="demo-banner" role="note">
      Demo — SYNTHETIC safeguarding data. All children, schools, dates and concerns are fabricated.
    </div>
  );
}
