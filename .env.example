# Copy this file to `.env` and fill in real values to enable the AI Ask panel.
# The compose stack picks them up automatically; the SPA's Ask panel hides
# itself when these are absent.

VOYAGE_API_KEY=
ANTHROPIC_API_KEY=
